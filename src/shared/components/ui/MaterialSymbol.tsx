import createIconSet from "@expo/vector-icons/createIconSet";
import glyphMap from "+/fonts/material-symbols.map.json";

const MaterialSymbol = createIconSet(
  glyphMap,
  "MaterialSymbolsOutlined-Regular",
  "MaterialSymbolsOutlined-Regular.ttf",
);

export default MaterialSymbol;
